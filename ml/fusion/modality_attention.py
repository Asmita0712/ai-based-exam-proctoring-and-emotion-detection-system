"""
Modality importance / attention analysis (Phase 3, Part C).

Answers questions like "how much does gaze contribute?" using values
derived from the trained fusion model and ablation experiments --
never invented, per the architecture rules.
"""


def compute_modality_importance(model, *args, **kwargs) -> dict:
    raise NotImplementedError("Implemented in Phase 3, alongside learned_fusion.py.")
